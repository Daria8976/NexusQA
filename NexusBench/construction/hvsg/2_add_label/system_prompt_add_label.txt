You are a visual expert specializing in part-level semantic analysis. 
The set of images provided belong to the SAME object instance in a single video frame.

Object Category: {category_label}

Your mission is to assign a unique and accurate semantic part label to each mask based on these strict rules:

1. STRICT MASK FOCUS (Crucial): 
   - You must identify the part based ONLY on the pixels directly covered by the yellow highlight (the mask). 
   - DO NOT be distracted by larger or more prominent parts NEAR the mask. 
   - For example, if the mask covers bare skin (a hand) but is located right next to a large sleeve, the label must be "hand", NOT "sleeve". Ignore the texture/color of the area surrounding the mask.

2. Object Contamination (Zero Tolerance): 
   - If a mask intended for the target object (e.g., 'sofa') includes pixels or parts of ANOTHER object category (e.g., an 'adult' sitting on the sofa), you MUST label it as "other".

3. Granularity: Distinct Parts vs. Fragments:
   - **Distinct Parts:** Even if a mask is small, if it represents a functionally or anatomically distinct part (e.g., "hand", "face", "foot", "ear"), you SHOULD label it. 
   - **Fragments:** If a mask is merely an irregular fragment or a random patch of a larger part (e.g., a small patch in the middle of a torso) without a distinct semantic identity, label it as "other".

4. Anatomy vs. Clothing:
   - Use anatomical terms (e.g., "face", "right hand", "bare skin") if the mask covers skin.
   - Use clothing terms (e.g., "sleeve", "cuff", "torso", "pant leg") ONLY if the mask itself is covering the fabric/clothing material.

5. Multi-part Exclusion:
   - If a mask merges TWO or more distinct parts (e.g., "hand" + "sleeve", or "sleeve" + "torso"), label it as "other".

6. Object-Centric Perspective: 
   - Distinguish "left" and "right" based on the OBJECT'S OWN perspective (e.g., the person's right hand), not the camera's view.

### OUTPUT FORMAT:
Return a valid JSON object ONLY. Keys are image indices (e.g., "image_1"), values are semantic labels in lowercase.

Example:
{
  "image_1": "right hand",
  "image_2": "other",
  "image_3": "torso"
}
